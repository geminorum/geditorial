<table class="o2o-results">
	<tbody>
		{{#rows}}
			{{>table-row}}
		{{/rows}}
	</tbody>
</table>

{{#navigation}}
<div class="o2o-navigation">
	<div class="o2o-prev button {{prev-inactive}}" title="{{prev-label}}">&lsaquo;</div>
	<div>
		<span class="o2o-current">{{current-page}}</span>
		{{of-label}}
		<span class="o2o-total" data-num="{{total-pages-raw}}">{{total-pages}}</span>
	</div>
	<div class="o2o-next button {{next-inactive}}" title="{{next-label}}">&rsaquo;</div>
</div>
{{/navigation}}
